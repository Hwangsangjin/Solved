#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    char Input;
    cin >> Input;
    cout << (int)Input;

    return 0;
}