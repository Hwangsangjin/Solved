#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    string Input;

    for (int i = 0; i < 100; ++i)
    {
        getline(cin, Input);
        cout << Input << "\n";
    }

    return 0;
}