#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    string Input;
    cin >> Input;

    cout << Input.length();

    return 0;
}